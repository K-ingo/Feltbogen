## Design (Feltbogen)

- Locked design system + screen references live in `docs/design/`.
- **Never** fetch `superdesign.dev` — use local HTML under `docs/design/`.
- Read `docs/design/TOKENS.md` before UI changes.
- Implement one screen at a time from Notion handoff + matching HTML file in `docs/design/desktop/` or `docs/design/mobile/`.
- Max one filled primary per viewport; accent text must be cream `#F9F5EB`.
